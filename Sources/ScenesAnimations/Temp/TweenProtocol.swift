/// `TweenProtocol` serves as a wrapper for `Tween` and provides a concrete object.
public protocol TweenProtocol {
    /// The time taken, in seconds, for a `Tween` to complete.
    var duration : Double { get }
}
